import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Admin — Agents Personnalisés' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
