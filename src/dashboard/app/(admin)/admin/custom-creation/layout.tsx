import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Admin — Création Sur Mesure' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
