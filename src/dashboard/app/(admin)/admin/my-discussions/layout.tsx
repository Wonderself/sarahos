import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Admin — Mes Discussions' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
