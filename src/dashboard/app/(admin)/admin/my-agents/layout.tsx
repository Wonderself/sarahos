import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Admin — Mes Outils IA' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
